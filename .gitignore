# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit (devenv auto-generates a symlink at .pre-commit-config.yaml,
# but the committed file is the source of truth for non-devenv users)

# Rust
target/
**/*.rs.bk
Cargo.lock.bak

# IDE
.idea/
.vscode/
.worktrees/
worktrees/
